```
static void Main(string[] args)
{
    RegistryKey rk = Registry.CurrentUser;
    RegistryKey test = rk.CreateSubKey("Software\\pxp1230.github.io\\Test", RegistryKeyPermissionCheck.ReadWriteSubTree);
    string name = test.GetValue("name") as string;
    if (!string.IsNullOrEmpty(name))
    {
        Console.WriteLine(name);
    }
    else
    {
        Console.WriteLine("没有找到");
        test.SetValue("name", "潘霄鹏");
    }
    test.Close();
    Console.ReadKey();
}
```